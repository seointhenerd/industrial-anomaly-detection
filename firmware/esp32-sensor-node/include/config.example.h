#pragma once

// Copy this file to config.h and fill in your actual values.
// config.h is gitignored — never commit real credentials.

#define WIFI_SSID     "your_wifi_ssid"
#define WIFI_PASSWORD "your_wifi_password"

// Run `ifconfig` (Mac/Linux) or `ipconfig` (Windows) to find your laptop's IP.
#define UDP_HOST "192.168.x.x"
#define UDP_PORT 5005
